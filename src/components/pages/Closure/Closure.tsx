import type { FC } from "react";
import ChildLayout from "../../templates/ChildLayout";

const Closure: FC = () => {
  return (
    <ChildLayout title="Letter of Credit Closure">
      <p>This is the Closure page content.</p>
    </ChildLayout>
  );
};

export default Closure;