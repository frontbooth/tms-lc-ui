import type { FC } from "react";
import ChildLayout from "../../templates/ChildLayout";

const Settlement: FC = () => {
  return (
    <ChildLayout title="Letter of Credit Settlement">
      <p>This is the Settlement page content.</p>
    </ChildLayout>
  );
};

export default Settlement;